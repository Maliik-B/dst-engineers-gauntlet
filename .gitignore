# Local-only AI workflow context — never commit (public portfolio repo)
CLAUDE.md
.claude/

# Extracted DST reference scripts (Klei's code — never commit)
.reference/

# Dev server logs (transient, written by the local dedicated server)
tools/*.log
tools/serverlog_*.txt

# Archives / OS noise
*.zip
Thumbs.db
desktop.ini
